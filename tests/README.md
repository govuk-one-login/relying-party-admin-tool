This folder contains both UI tests and integration tests (coming soon). Check the README in their respective directories for information on how to run them.

The Dockerfile in this folder is only used to build both test folders into one container and run them in the test pipeline.
